query dataTableRowsPage($environmentId: ID!, $tableId: ID!, $limit: Int, $offset: Int) {
    dataTableRowsPage(environmentId: $environmentId, tableId: $tableId, limit: $limit, offset: $offset) {
        items {
            id
            values
        }
        hasMore
        nextOffset
    }
}
