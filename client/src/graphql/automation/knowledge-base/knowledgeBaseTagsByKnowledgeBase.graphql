query knowledgeBaseTagsByKnowledgeBase($environmentId: ID!, $workspaceId: ID!) {
    knowledgeBaseTagsByKnowledgeBase(environmentId: $environmentId, workspaceId: $workspaceId) {
        knowledgeBaseId
        tags {
            id
            name
        }
    }
}
