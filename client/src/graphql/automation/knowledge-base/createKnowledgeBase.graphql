mutation createKnowledgeBase($knowledgeBase: KnowledgeBaseInput!, $environmentId: ID!, $workspaceId: ID!) {
    createKnowledgeBase(knowledgeBase: $knowledgeBase, environmentId: $environmentId, workspaceId: $workspaceId) {
        id
        name
    }
}
