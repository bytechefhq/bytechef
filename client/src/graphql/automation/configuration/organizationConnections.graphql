query organizationConnections($environmentId: ID) {
    organizationConnections(environmentId: $environmentId) {
        id
        name
        componentName
        environmentId
        visibility
        createdBy
        createdDate
        lastModifiedDate
    }
}
