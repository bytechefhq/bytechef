mutation setConnectionProjects($workspaceId: ID!, $connectionId: ID!, $projectIds: [ID!]!) {
  setConnectionProjects(workspaceId: $workspaceId, connectionId: $connectionId, projectIds: $projectIds)
}
