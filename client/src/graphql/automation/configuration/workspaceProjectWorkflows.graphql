query workspaceProjectWorkflows($workspaceId: ID!) {
    workspaceProjectWorkflows(workspaceId: $workspaceId) {
        projectId
        projectName
        projectWorkflowId
        workflowId
        workflowLabel
    }
}
