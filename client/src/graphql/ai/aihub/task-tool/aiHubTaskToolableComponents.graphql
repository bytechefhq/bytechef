query aiHubTaskToolableComponents($workspaceId: ID!) {
    aiHubTaskToolableComponents(workspaceId: $workspaceId) {
        componentName
        componentVersion
        connectionRequired
        description
        icon
        title
        tools {
            description
            name
            title
        }
    }
}
