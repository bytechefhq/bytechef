mutation createAiSkillFromInstructions($name: String!, $description: String, $instructions: String!) {
    createAiSkillFromInstructions(name: $name, description: $description, instructions: $instructions) {
        id
        name
        description
        createdDate
        lastModifiedDate
    }
}
