query mcpToolsByComponentId($mcpComponentId: ID!) {
    mcpToolsByComponentId(mcpComponentId: $mcpComponentId) {
        id
        enabled
        name
        title
        mcpComponentId
        parameters
        version
    }
}
