query mcpComponentsByServerId($mcpServerId: ID!) {
    mcpComponentsByServerId(mcpServerId: $mcpServerId) {
        id
        componentName
        componentVersion
        title
        connectionId,
        lastModifiedDate
        mcpServerId
        mcpTools {
            id,
            enabled
            mcpComponentId
            name
            parameters
            title
            version
        }
        version
    }
}
