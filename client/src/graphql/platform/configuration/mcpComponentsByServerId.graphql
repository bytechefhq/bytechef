query mcpComponentsByServerId($mcpServerId: ID!) {
    mcpComponentsByServerId(mcpServerId: $mcpServerId) {
        id
        componentName
        componentVersion
        title
        connectionId,
        mcpServerId
        mcpTools {
            id,
            mcpComponentId
            name
        }
        version
    }
}
