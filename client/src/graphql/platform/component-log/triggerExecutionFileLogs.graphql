query triggerExecutionFileLogs($triggerExecutionId: ID!, $filter: LogFilterInput, $page: Int, $size: Int) {
    triggerExecutionFileLogs(triggerExecutionId: $triggerExecutionId, filter: $filter, page: $page, size: $size) {
        content {
            timestamp
            level
            componentName
            componentOperationName
            taskExecutionId
            triggerExecutionId
            message
            exceptionType
            exceptionMessage
            stackTrace
        }
        totalElements
        totalPages
        pageNumber
        pageSize
        hasNext
        hasPrevious
    }
}
