query embeddedVariables($environmentId: ID!) {
    embeddedVariables(environmentId: $environmentId) {
        id
        name
        value
        environmentId
        createdBy
        createdDate
        lastModifiedBy
        lastModifiedDate
    }
}
