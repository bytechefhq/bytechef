import {Edge} from '@xyflow/react';

const defaultEdges: Edge[] = [
    {
        id: 'manual=>2',
        source: 'manual',
        target: '2',
        type: 'placeholder',
    },
];

export default defaultEdges;
