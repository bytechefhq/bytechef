import {Edge} from 'reactflow';

const defaultEdges: Edge[] = [
    {
        id: '1=>2',
        source: '1',
        target: '2',
        type: 'placeholder',
    },
];

export default defaultEdges;
