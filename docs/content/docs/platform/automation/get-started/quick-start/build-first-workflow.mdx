---
title: "Build First Workflow"
description: "Build and run your first ByteChef workflow in a few minutes. No connections or credentials needed."
---

In this guide you build the same workflow as the [Learn ByteChef by doing](https://www.bytechef.io/workflow-templates/learn-bytechef-by-doing) template: a trigger, an HTTP call to a public API, and a logger that prints the result. It needs no connections, so it runs the moment you finish it.

You will learn:

- How to create a project and a workflow
- How to add and configure components
- How to pass data between steps with an expression
- How to test a workflow and read its execution log

## Before you start

Sign in to [ByteChef Cloud](https://app.bytechef.io/) or run ByteChef yourself. The quickest self-hosted option is one command, with [Docker Desktop](https://www.docker.com/products/docker-desktop/) installed:

```bash
docker run --name bytechef -it -p 8080:8080 \
    --env BYTECHEF_DATABASE=h2 \
    -v ~/.bytechef:/root/.bytechef \
    docker.bytechef.io/bytechef/bytechef:latest
```

Open [http://localhost:8080/login](http://localhost:8080/login) and click **Create account**. This runs on an embedded H2 database and is meant for evaluation. For anything you intend to keep, use PostgreSQL through [Docker Compose](/platform/use-bytechef/self-hosted/installation/local-docker), or see the guides for [Kubernetes](/platform/use-bytechef/self-hosted/installation/kubernetes), [AWS EC2](/platform/use-bytechef/self-hosted/installation/aws-ec2), [AWS ECS](/platform/use-bytechef/self-hosted/installation/aws-ecs), [Azure](/platform/use-bytechef/self-hosted/installation/azure), [Google Cloud](/platform/use-bytechef/self-hosted/installation/google-cloud) and [DigitalOcean](/platform/use-bytechef/self-hosted/installation/digitalocean).

On first sign-in you land in the **Automation** workspace.

## 1. Create a project

A project is the container that holds your workflows.

<Steps>
<Step>

Open the **Projects** page and click **Create Project**. The button reads **New Project** once you already have projects.

</Step>
<Step>

Enter a project name, for example `Getting started`. Optionally add a description, choose a category, and add tags. Then click **Save**.

{/* screenshot: the Create Project dialog with the name "Getting started" filled in. Save as build-first-workflow/create-project.png */}

</Step>
</Steps>

## 2. Create a workflow

<Steps>
<Step>

On your new project's row, click **+ Workflow**.

</Step>
<Step>

Enter the label `Learn ByteChef by doing` and click **Save**. ByteChef opens the new workflow in the editor with a **Manual** trigger already in place.

{/* screenshot: the new workflow in the editor, showing the Manual trigger and the + button. Save as build-first-workflow/workflow-editor.png */}

</Step>
</Steps>

The Manual trigger starts the workflow when you click **Test** or **Run**. The template uses the same trigger. When you want the workflow to run on its own, click the trigger and swap it for a **Schedule** trigger.

## 3. Fetch a random quote

<Steps>
<Step>

Click the **+** button below the trigger, search for **HTTP Client**, and select it.

</Step>
<Step>

Choose the **GET** action.

</Step>
<Step>

In the properties panel, set **URI** to:

```
https://dummyjson.com/quotes/random
```

Leave **Response Type** on **JSON** and everything else at its default. There is no connection to create: the API is public.

{/* screenshot: the HTTP Client properties panel with the GET action selected and the URI filled in. Save as build-first-workflow/http-client.png */}

</Step>
<Step>

Optionally rename the step by clicking its label at the top of the panel, for example `Get a random quote`. The step's **name**, `httpClient_1`, is what other steps use to reference its output; it does not change when you edit the label.

</Step>
</Steps>

## 4. Log the quote

<Steps>
<Step>

Click the **+** button below the HTTP Client step, search for **Logger**, and select it.

</Step>
<Step>

Choose the **Info** action.

</Step>
<Step>

In the **Text** field, enter the expression:

```
${httpClient_1.quote}
```

This is a data pill: it pulls the `quote` field out of the JSON the HTTP Client step returned. You can also open the field's data picker and click the `quote` field under `httpClient_1` to insert the same expression.

{/* screenshot: the Logger properties panel with the Info action and the ${httpClient_1.quote} expression in the Text field. Save as build-first-workflow/logger.png */}

</Step>
</Steps>

## 5. Test the workflow

Click **Test** at the top right of the editor. ByteChef runs the workflow once:

- The Manual trigger starts it
- The HTTP Client step calls the quotes API
- The Logger step prints the quote

The execution panel opens below the canvas. Click the Logger step to see the quote in its output, and click the HTTP Client step to see the full JSON it received.

{/* screenshot: the execution panel after a successful test, with the Logger step selected and the quote visible. Save as build-first-workflow/test-execution.png */}

Run it again and you get a different quote.

## What you have built

```
Manual Trigger → HTTP Client (GET) → Logger (Info)
```

This is the shape of every ByteChef workflow:

- **Trigger**: something starts the workflow
- **Components**: do the work, such as fetching, creating or sending
- **Data pills**: pass data between steps with `${stepName.fieldName}`. See [Data Pills & Input Modes](/platform/automation/build/workflows/data-pills)

The same workflow is available as a template. To import it instead of building it, open the [Learn ByteChef by doing](https://www.bytechef.io/workflow-templates/learn-bytechef-by-doing) page, click **Use template**, and then click **Run** in the editor.

## Next steps

- [Build First Agent](/platform/automation/get-started/quick-start/build-first-agent): add an AI agent with a tool, with one API key
- [Star Repository on GitHub](/platform/automation/get-started/quick-start/star-repository-on-github): your first workflow with a connection
- [Configure Workflow Trigger](/platform/automation/get-started/quick-start/configure-workflow-trigger): a real trigger, a deployment and monitoring
- [Browse 250+ components](/reference/components)
- [Templates library](https://www.bytechef.io/workflow-templates)
