{
  "label": "Slack channel → Knowledge Base",
  "description": "Daily snapshot of a Slack channel's last 24h of messages. Concatenates them as markdown and loads as a single Knowledge Base document per day. PREREQUISITE: a Slack channel-history action; the current in-tree Slack component (server/libs/modules/components/slack/) only exposes send-message actions, so the fetchHistory_1 task type below is a placeholder that will need to point at the real action when it lands.",
  "inputs": [
    {
      "name": "knowledgeBaseId",
      "label": "Target Knowledge Base",
      "type": "INTEGER",
      "required": true
    },
    {
      "name": "channelId",
      "label": "Slack Channel ID",
      "description": "The channel to capture. In Slack, right-click the channel → Copy link → the id is the C-prefixed segment.",
      "type": "STRING",
      "required": true
    }
  ],
  "triggers": [
    {
      "label": "Daily at 04:00 UTC",
      "name": "trigger_1",
      "type": "schedule/v1/cron",
      "parameters": {
        "expression": "0 0 4 * * *",
        "timezone": "UTC"
      }
    }
  ],
  "tasks": [
    {
      "label": "Fetch last 24h of channel history",
      "name": "fetchHistory_1",
      "type": "slack/v1/fetchConversationHistory",
      "parameters": {
        "channel": "${channelId}",
        "oldest": "${dateMath('now', '-1d')}"
      },
      "metadata": {
        "ui": {
          "note": "TODO: the Slack component needs a channel-history action. Until then, this task type is a placeholder. Replace with the real action identifier once shipped."
        }
      }
    },
    {
      "label": "Format messages as markdown",
      "name": "formatMd_1",
      "type": "logger/v1/info",
      "parameters": {
        "text": "TODO: replace this stub with a script step or a custom transform that joins the messages array into a markdown document; the result becomes the input to the KB load below."
      },
      "metadata": {
        "ui": {
          "note": "Replace with a script/v1/exec step or a dedicated formatter task once the channel-history action is wired up."
        }
      }
    },
    {
      "label": "Load daily snapshot into KB",
      "name": "loadSnapshot_1",
      "type": "ai_vectorstore_knowledgebase/v1/load",
      "parameters": {
        "knowledgeBaseId": "${knowledgeBaseId}"
      },
      "metadata": {
        "ui": {
          "note": "Pick a string-based Document Reader cluster element and feed it ${formatMd_1.output}."
        }
      }
    }
  ]
}
