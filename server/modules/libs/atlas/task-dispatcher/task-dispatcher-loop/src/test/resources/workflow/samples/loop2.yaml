label: Loop 1

outputs:
    - name: sumResult
      value: ${sumResult}

tasks:
  - type: loop
    list: "${range(1,10)}"
    itemVar: item1
    iteratee:
      type: loop
      list: "${range(1,item1)}"
      itemVar: item2
      iteratee:
        name: sumVar1
        type: core/var
        value: ${item1 + '_' + item2}
