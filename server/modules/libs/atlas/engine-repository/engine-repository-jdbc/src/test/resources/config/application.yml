spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/integri
    username: postgres
    password: postgres
    hikari:
      poolName: Hikari
#      auto-commit: false
  sql:
    init:
      mode: always
      platform: postgres
