dependencies {
    compileOnly "jakarta.servlet:jakarta.servlet-api"

    implementation "org.slf4j:slf4j-api"
    implementation "org.springframework:spring-web"
    implementation "org.springframework:spring-webmvc"
    implementation project(":modules:libs:hermes:auth:auth-service")

    testImplementation "org.springframework:spring-jdbc"
    testImplementation "org.springframework.boot:spring-boot-starter-web"
    testImplementation project(":modules:libs:hermes:encryption:encryption-api")
    testImplementation project(":modules:libs:hermes:auth:auth-repository:auth-repository-api")
}
