dependencies {
    api project(":modules:libs:hermes:auth:auth-repository:auth-repository-api")

    implementation "org.springframework:spring-jdbc"
    implementation project(":modules:libs:hermes:encryption:encryption-api")

    testImplementation "org.springframework.boot:spring-boot-starter-jdbc"
    testImplementation project(":modules:libs:test-support")
}
