management:
  endpoints:
    web:
      base-path: /management
      exposure:
        include:
          [
            "caches",
            "configprops",
            "env",
            "health",
            "info",
            "logfile",
            "loggers",
            "threaddump",
          ]
  info:
    git:
      mode: full
  health:
    #    db:
    #      enabled: false
    group:
      liveness:
        include: livenessState
      readiness:
        include: readinessState,db
    rabbit:
      enabled: false
  metrics:
    #    export:
    #      prometheus:
    #        enabled: true
    #        step: 60
    enable:
      http: true
      jvm: true
      logback: true
      process: true
      system: true
    distribution:
      percentiles-histogram:
        all: true
      percentiles:
        all: 0, 0.5, 0.75, 0.95, 0.99, 1.0
    tags:
      application: ${spring.application.name}
    web:
      server:
        request:
          autotime:
            enabled: true

spring:
  application:
    name: server-app
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false
      write-null-map-values: false
    date-format: ${atlas.serialization.date-format}
    time-zone: UTC
  profiles:
    active: #spring.profiles.active#
    group:
      dev:
        - dev
  output:
    ansi:
      console-available: true
  spring:
    rabbitmq:
      listener:
        direct:
          prefetch: 1
  task:
    execution:
      thread-name-prefix: server-application-task-
      pool:
        core-size: 2
        max-size: 50
        queue-capacity: 10000
    scheduling:
      thread-name-prefix: server-application-scheduling-
      pool:
        size: 2

atlas:
  context:
    delete-listener:
      enabled: false
  coordinator:
    enabled: true
  message-broker:
    provider: jms
  persistence:
    provider: jdbc
  serialization:
    date-format: yyyy-MM-dd'T'HH:mm:ss.SSSZZ
  storage:
    provider: file
    fileStorageDir: /tmp/bytechef/files
  worker:
    enabled: true
    subscriptions:
      tasks: "5"
  workflow-repository:
    database:
      enabled: true
    classpath:
      enabled: true
    git:
      enabled: false
      url: https://github.com/bytechef/bytechef-workflows.git
      search-paths: demo/

org:
  jobrunr:
    background-job-server:
      enabled: true
