---
# Conditionally disable springdoc on missing api-docs profile
spring:
  config:
    activate:
      on-profile: '!api-docs'
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
---
billing:
  enabled: false
  stripe:
    customer-portal-url: https://billing.stripe.com/p/login/test_8x214mfs66RX9fP6BV3Je00
    secret-key:
    product-starter-id: prod_UQkzkfmlljRg3J
    product-usage-id: prod_UQlAqLdV50vcMc
    product-growth-id: prod_UQl6ZIubWx3VS4
    meter-event-name: tenant_task_executions
    webhook-secret:
    success-url: http://localhost:5173/automation/settings/billing?checkout=success
    cancel-url: http://localhost:5173/automation/settings/billing

github-proxy:
  baseUrl: https://github-proxy.bytechef.io/

info:
  ai:
    hub:
      enabled: ${bytechef.ai.hub.enabled:false}
    copilot:
      enabled: ${bytechef.ai.copilot.enabled}
    gateway:
      enabled: ${bytechef.ai.gateway.enabled:false}
    knowledgeBase:
      enabled: ${bytechef.ai.knowledge-base.enabled:false}
  contextStore:
    enabled: ${bytechef.context-store.enabled:false}
  analytics:
    enabled: ${bytechef.analytics.enabled}
    postHog:
      apiKey: ${post-hog.api-key:}
      host: ${post-hog.host:}
  billing:
    enabled: ${billing.enabled}
  application:
    edition: ${bytechef.edition:}
  component:
    customComponent:
      javaEnabled: ${bytechef.component.custom-component.java-enabled:true}
  featureFlags: ${bytechef.feature-flags}
  helpHub:
    enabled: ${bytechef.help-hub.enabled}
    commandBar:
      orgId: ${command-bar.org-id:}
  signUp:
    activationRequired: ${bytechef.sign-up.activation-required}
    enabled: ${bytechef.sign-up.enabled}
  socialLogin:
    enabled: ${bytechef.security.social-login.enabled}
  userGuiding:
    containerId: ${bytechef.user-guiding.container-id:}
    enabled: ${bytechef.user-guiding.enabled}
  workflow:
    codeWorkflow:
      javaEnabled: ${bytechef.workflow.code-workflow.java-enabled:true}

logging:
  level:
    ROOT: INFO
    org.quartz: WARN
    org.jline: ERROR
    com.bytechef.ee.platform.scheduler.tenant: ERROR

management:
  endpoints:
    web:
      exposure:
        include:
          - configprops
          - env
          - health
          - info
          - logfile
          - loggers
          - prometheus
          - threaddump
          - caches
          - liquibase
  endpoint:
    env:
      show-values: when_authorized
    health:
      showDetails: always
      probes:
        enabled: true
      group:
        liveness:
          include: livenessState
        readiness:
          include: readinessState,db
  health:
    neo4j:
      enabled: false
    redis:
      enabled: false
  info:
    git:
      mode: full
    env:
      enabled: true
  metrics:
    enable:
      http: true
      jvm: true
      logback: true
      process: true
      system: true
    distribution:
      percentiles-histogram:
        all: true
      percentiles:
        all: 0, 0.5, 0.75, 0.95, 0.99, 1.0
    tags:
      application: ${spring.application.name}
  observations:
    key-values:
      application: ${spring.application.name}
  opentelemetry:
    tracing:
      export:
        otlp:
          endpoint: ${bytechef.observability.tracing.endpoint}
    logging:
      export:
        otlp:
          endpoint: ${bytechef.observability.logging.endpoint}
  logging:
    export:
      enabled: ${bytechef.observability.logging.enabled}
  tracing:
    export:
      enabled: ${bytechef.observability.tracing.enabled}
    sampling:
      probability: 1.0
  otlp:
    metrics:
      export:
        enabled: ${bytechef.observability.metrics.enabled}
        step: 10s
        url: ${bytechef.observability.metrics.endpoint}

openapi:
  openAPIDefinition:
    base-path:
      automation: /api/automation
      embedded: /api/embedded
      platform: /api/platform

spring:
  config:
    import: classpath:config/application-bytechef.yml
  ai:
    chat:
      client:
        enabled: ${bytechef.ai.copilot.enabled}
    model:
      audio:
        speech: none
        transcription: none
      chat: none
      embedding: none
      image: none
      moderation: none
    vectorstore:
      type: none
      pgvector:
        index-type: hnsw
        distance-type: cosine_distance
        dimensions: 1536
        max-document-batch-size: 10000
        initialize-schema: true
  application:
    name: server-app
  autoconfigure:
    exclude:
      com.embabel.agent.autoconfigure.platform.AgentPlatformAutoConfiguration,
      org.springaicommunity.agents.claude.autoconfigure.ClaudeAgentAutoConfiguration,
      org.springaicommunity.agents.claude.autoconfigure.ClaudeHookAutoConfiguration,
      org.springframework.ai.model.anthropic.autoconfigure.AnthropicChatAutoConfiguration,
      org.springframework.ai.model.openai.autoconfigure.OpenAiChatAutoConfiguration,
      org.springframework.ai.vectorstore.pgvector.autoconfigure.PgVectorStoreAutoConfiguration,
      org.springframework.boot.data.redis.autoconfigure.DataRedisAutoConfiguration,
      org.springframework.boot.neo4j.autoconfigure.Neo4jAutoConfiguration
  batch:
    job:
      enabled: false
  cache:
    type: ${bytechef.cache.provider}
  cloud:
    aws:
      s3:
        enabled: false
        vector:
          enabled: false
      sqs:
        enabled: false
  data:
    redis:
      database: ${bytechef.redis.database}
      host: ${bytechef.redis.host}
      port: ${bytechef.redis.port}
      password: ${bytechef.redis.password}
      timeout: ${bytechef.redis.timeout}
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: ${bytechef.datasource.url}
    username: ${bytechef.datasource.username}
    password: ${bytechef.datasource.password}
    hikari:
      connection-test-query: SELECT 1
      poolName: Hikari
  h2:
    console:
      enabled: false
  graphql:
    schema:
      locations: classpath*:graphql/**/
  jmx:
    enabled: false
  kafka:
    bootstrap-servers: ${bytechef.kafka.bootstrap-servers}
    consumer:
      group-id: ${bytechef.kafka.consumer.group-id}
  mail:
    host: ${bytechef.mail.host}
    port: ${bytechef.mail.port}
    username: ${bytechef.mail.username}
    password: ${bytechef.mail.password}
    properties:
      mail:
        debug: ${bytechef.mail.debug}
        smtp:
          auth: ${bytechef.mail.auth}
          starttls:
            enable: ${bytechef.mail.starttls.enable}
            required: ${bytechef.mail.starttls.required}
        transport:
          protocol: ${bytechef.mail.protocol}
    protocol: ${bytechef.mail.protocol}
    ssl:
      enabled: ${bytechef.mail.ssl.enabled}
  mvc:
    async:
      request-timeout: 10m
  liquibase:
    contexts: mono, #spring.profiles.active#
    enabled: ${bytechef.upgrade.enabled:true}
    parameters:
      billing.enabled: ${billing.enabled:false}
  output:
    ansi:
      enabled: always
  profiles:
    active: #spring.profiles.active#
    group:
      dev:
        - dev
        - api-docs
  rabbitmq:
    host: ${bytechef.rabbitmq.host}
    listener:
      direct:
        prefetch: 1
    password: ${bytechef.rabbitmq.password}
    port: ${bytechef.rabbitmq.port}
    username: ${bytechef.rabbitmq.username}
  servlet:
    multipart:
      # Match the 64 MiB cap enforced by CreateBinaryAssetFileToolCallback before base64 decode. Spring Boot's
      # 1 MB / 10 MB defaults would otherwise reject ordinary asset uploads with a generic 500 long before the
      # tool-side cap fires.
      max-file-size: 64MB
      max-request-size: 80MB
  shell:
    interactive:
      enabled: false
    history:
      enabled: false
    noninteractive:
      enabled: false
  quartz:
    auto-startup: false
    job-store-type: jdbc
    jdbc:
      initialize-schema: never
    properties:
      org.quartz.jobStore.driverDelegateClass: org.quartz.impl.jdbcjobstore.PostgreSQLDelegate
  task:
    execution:
      thread-name-prefix: server-app-task-
      pool:
#        core-size: 2
        max-size: 50
        queue-capacity: 10000
    scheduling:
      thread-name-prefix: server-app-scheduling-
      pool:
        size: 2
  threads:
    virtual:
      enabled: true

server:
  servlet:
    session:
      cookie:
        http-only: true

springdoc:
  show-actuator: true
  swagger-ui:
    operationsSorter: method
    tags-sorter: alpha

templates:
  owner: bytechefhq
  ref: master
  repo: bytechef-workflows
  submission-form:
    projects: https://forms.gle/W6zsUuwd7qPtoE836
    workflows: https://forms.gle/xA8yyUSzKkHYajHx5
