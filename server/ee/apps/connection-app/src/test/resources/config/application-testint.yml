server:
  port: 9999

openapi:
  openAPIDefinition:
    base-path:
      automation: /api/automation
      embedded: /api/embedded
      platform: /api/platform

spring:
  liquibase:
    contexts: connection

bytechef:
  encryption:
    provider: filesystem
