server:
  port: 9999

bytechef:
  edition: EE
  message-broker:
    provider: memory
  file-storage:
    provider: jdbc
  oauth2:
    redirect-uri:
  workflow:
    output-storage:
      provider: jdbc
