server:
  port: 9999

bytechef:
  edition: EE
  message-broker:
    provider: memory
  file-storage:
    provider: jdbc
  oauth2:
    redirect-uri:
  worker:
    enabled: false
  workflow:
    output-storage:
      provider: jdbc
