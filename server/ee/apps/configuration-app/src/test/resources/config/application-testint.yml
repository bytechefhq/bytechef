server:
  port: 9999

openapi:
  openAPIDefinition:
    base-path:
      automation: /api/automation
      embedded: /api/embedded
      platform: /api/platform

bytechef:
  workflow:
    repository:
      jdbc:
        enabled: true
    output-storage:
      provider: base64
