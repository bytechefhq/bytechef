server:
  port: 9999

openapi:
  openAPIDefinition:
    base-path:
      automation: /api/automation
      embedded: /api/embedded
      platform: /api/platform

bytechef:
  data-storage:
    provider: jdbc
  edition: EE
  file-storage:
    provider: jdbc
  message-broker:
    provider: memory
  workflow:
    output-storage:
      provider: jdbc
