server:
  port: 9999

openapi:
  openAPIDefinition:
    base-path:
      automation: /api/automation
      embedded: /api/embedded
      platform: /api/platform

bytechef:
  data-storage:
    provider: jdbc
  file-storage:
    provider: base64
  message-broker:
    provider: jms
  workflow:
    output-storage:
      provider: base64
