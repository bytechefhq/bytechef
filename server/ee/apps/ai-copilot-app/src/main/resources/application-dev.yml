spring:
  devtools:
    restart:
      enabled: true

bytechef:
  ai:
    copilot:
      vectorstore:
        pgvector:
          url: jdbc:postgresql://localhost:5433/bytechef_copilot
          username: postgres
          password: postgres
