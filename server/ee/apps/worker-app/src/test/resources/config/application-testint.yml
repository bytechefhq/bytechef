server:
  port: 9999

bytechef:
  discovery-service:
    provider: redis
  data-storage:
    provider: db
  file-storage:
    provider: base64
  message-broker:
    provider: jms
  workflow:
    output-storage:
      provider: base64
