<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- ChangeSet to create the vector extension -->
    <changeSet id="00000000000001" author="Marko Kriskovic">
        <preConditions onFail="MARK_RAN">
            <not>
                <sqlCheck expectedResult="0">
                    SELECT COUNT(*) FROM pg_extension;
                </sqlCheck>
            </not>
        </preConditions>
        <sql>
            CREATE EXTENSION IF NOT EXISTS vector;
            CREATE EXTENSION IF NOT EXISTS hstore;
            CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
        </sql>
    </changeSet>

    <!-- ChangeSet to create the workflow table -->
    <changeSet id="00000000000001-1" author="Marko Kriskovic">
        <createTable tableName="vector_store">
            <column name="id" type="UUID" defaultValue="uuid_generate_v4()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="content" type="text"/>
            <column name="metadata" type="JSONB"/>
            <column name="embedding" type="VECTOR(1536)">
                <constraints nullable="true"/>
            </column>
        </createTable>
        <!-- Create an index for vector similarity search -->
        <sql>
            CREATE INDEX idx_documentation_embedding ON vector_store USING hnsw (embedding vector_cosine_ops);
        </sql>
    </changeSet>
</databaseChangeLog>
