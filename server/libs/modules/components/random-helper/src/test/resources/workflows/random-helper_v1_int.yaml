label: Random Int

outputs:
  - name: randomNumber
    value: ${randomNumber}

tasks:
  - name: randomNumber
    label: Generate a random integer
    type: random/v1/randomInt
    startInclusive: 0
    endInclusive: 1000
