---
label: "Random Int"
outputs:
- name: "randomNumber"
  value: "${randomNumber}"
tasks:
- name: "randomNumber"
  label: "Generate a random integer"
  type: "randomHelper/v1/randomInt"
  parameters:
    startInclusive: 0
    endInclusive: 1000
