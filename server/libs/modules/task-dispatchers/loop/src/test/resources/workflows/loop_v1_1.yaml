---
label: "Loop 1"
tasks:
- name: "loop-1"
  type: "loop/v1"
  parameters:
    list: "${range(1,10)}"
    itemVar: "item1"
    iteratee:
      name: "sumVar1"
      type: "var"
      parameters:
        value: "${1 + item1}"
