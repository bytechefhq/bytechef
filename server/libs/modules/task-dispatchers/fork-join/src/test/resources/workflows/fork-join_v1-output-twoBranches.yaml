---
label: "Fork Join Task Output"
outputs:
- name: "branch0Result"
  value: "${forkJoin_1.branch_0}"
- name: "branch1Result"
  value: "${forkJoin_1.branch_1}"
tasks:
- name: "forkJoin_1"
  type: "fork-join/v1"
  parameters:
    branches:
    - - name: "lastTask0"
        type: "var/v1/set"
        parameters:
          value: "branch zero output"
    - - name: "lastTask1"
        type: "var/v1/set"
        parameters:
          value: "branch one output"
