---
label: "Hello for-each"
tasks:
- name: "each-1"
  type: "each/v1"
  parameters:
    list: "${range(1,25)}"
    itemVar: "item1"
    iteratee:
      name: "each-1"
      type: "each/v1"
      parameters:
        list: "${range(1,25)}"
        itemVar: "item2"
        iteratee:
          name: "var1"
          type: "var"
          parameters:
            value: "${item1 + '_' + item2}"
