---
label: "Graph Forward"
outputs:
- name: "result"
  value: "${graph_1}"
tasks:
- name: "graph_1"
  type: "graph/v1"
  parameters:
    nodes:
    - name: "aTask"
      type: "var/v1/set"
      parameters:
        value: "A output"
    - name: "bTask"
      type: "var/v1/set"
      parameters:
        value: "B output"
    transitions:
    - from: "aTask"
      to: "bTask"
