---
label: "Graph Budget Exhausted"
tasks:
- name: "graph_1"
  type: "graph/v1"
  parameters:
    maxTransitions: 3
    nodes:
    - name: "X"
      tasks:
      - name: "xTask"
        type: "var/v1/set"
        parameters:
          value: "x"
      next: "Y"
    - name: "Y"
      tasks:
      - name: "yTask"
        type: "var/v1/set"
        parameters:
          value: "y"
      next: "X"
