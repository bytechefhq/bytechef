openapi: 3.0.1
info:
  title: OpenAPI definition
  version: v1
servers:
  - url: http://localhost:5173/api
paths:
  /definitions/task-dispatchers:
    get:
      description: Returns all task dispatcher definitions.
      tags:
        - task-dispatcher-definitions
      operationId: getTaskDispatcherDefinitions
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                   $ref: '#/components/schemas/TaskDispatcherDefinition'
components:
  schemas:
    TaskDispatcherDefinition:
      $ref: '../hermes-task-dispatcher-api/task_dispatcher_definition_spec.yaml#/schemas/TaskDispatcherDefinition'
