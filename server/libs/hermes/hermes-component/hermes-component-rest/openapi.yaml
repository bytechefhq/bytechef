openapi: 3.0.1
info:
  title: OpenAPI definition
  version: v1
servers:
  - url: http://localhost:5173/api
paths:
  /definitions/components:
    get:
      description: Returns all component definitions.
      tags:
        - component-definitions
      operationId: getComponentDefinitions
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ComponentDefinition'
  /definitions/connections:
    get:
      description: Returns all connection definitions.
      tags:
        - connection-definitions
      operationId: getConnectionDefinitions
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Connections'
components:
  schemas:
    ComponentDefinition:
      $ref: '../hermes-component-api/component_definition_spec.yaml#/schemas/ComponentDefinition'
    Connections:
      type: object
      description: TODO
      properties:
        connections:
          description: TODO
          type: array
          items:
            $ref: '../hermes-component-api/component_definition_spec.yaml#/schemas/ConnectionDefinition'
        name:
          description: TODO
          type: string
        version:
          type: integer
          format: int32
