# Test-only GraphQL base operations and minimal type stubs for GraphQL slice tests.

# Base operation types so that 'extend type Query/Mutation' from main resources can merge.
# Note: Query is already defined in base.graphqls from platform-configuration, only Mutation needs to be defined
type Mutation {
    _placeholder: Boolean
}

# Minimal type stubs referenced by data-table.graphqls
scalar Long

type Tag {
    id: ID
    name: String
}

input TagInput {
    id: ID
    name: String!
}

scalar Map
