---
openapi: "3.0.1"
info:
  title: "The Automation Configuration Internal API"
  version: "1"
servers:
  - url: "/api/automation/internal"
tags:
  - name: "category"
    description: "The Automation Category API"
  - name: "component-definition"
    description: "The Automation Component Definition Internal API"
  - name: "connection"
    description: "The Automation Connection Internal API"
  - name: "connection-tag"
    description: "The Automation Connection Tag Internal API"
  - name: "project-deployment-tag"
    description: "The Automation Project Deployment Tag Internal API"
  - name: "project-deployment"
    description: "The Automation Project Deployment Internal API"
  - name: "project-tag"
    description: "The Automation Project Tag Internal API"
  - name: "project"
    description: "The Automation Project Internal API"
  - name: "webhook-trigger-test"
    description: "The Automation Webhook Trigger Internal API"
  - name: "workflow"
    description: "The Automation Workflow Internal API"
  - name: "workspace"
    description: "The Automation Workspace Internal API"
paths:
  /component-definitions:
    get:
      description: "Get all component definitions."
      summary: "Get all component definitions"
      tags:
        - "component-definition"
      operationId: "getComponentDefinitions"
      parameters:
        - name: "actionDefinitions"
          description: "Use for filtering components which define action definitions."
          in: "query"
          required: false
          schema:
            type: "boolean"
        - name: "connectionDefinitions"
          description: "Use for filtering components which define connection definitions."
          in: "query"
          required: false
          schema:
            type: "boolean"
        - name: "triggerDefinitions"
          description: "Use for filtering components which define trigger definitions."
          in: "query"
          required: false
          schema:
            type: "boolean"
        - name: "include"
          description: "The list of component names to include in the result."
          in: "query"
          required: false
          schema:
            type: "array"
            items:
              type: "string"
      responses:
        "200":
          description: "Successful operation."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "../../../../platform/platform-configuration/platform-configuration-rest/platform-configuration-rest-impl/openapi.yaml/#/components/schemas/ComponentDefinitionBasic"
  /connections:
    post:
      description: "Create a new connection."
      summary: "Create a new connection"
      tags:
        - "connection"
      operationId: "createConnection"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Connection"
        required: true
      responses:
        "200":
          description: "The connection id."
          content:
            application/json:
              schema:
                type: integer
                format: int64
  /connections/tags:
    get:
      description: "Get connection tags."
      summary: "Get connection tags"
      tags:
        - "connection-tag"
      operationId: "getConnectionTags"
      responses:
        "200":
          description: "The list of connection tags."
          content:
            application/json:
              schema:
                description: "The response object that contains the array of tags."
                type: "array"
                items:
                  $ref: "#/components/schemas/Tag"
  /connections/{id}:
    delete:
      description: "Delete a connection."
      summary: "Delete a connection"
      tags:
        - "connection"
      operationId: "deleteConnection"
      parameters:
        - name: "id"
          description: "The id of a connection."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      responses:
        "204":
          description: "Successful operation."
    get:
      description: "Get a connection by id."
      summary: "Get a connection by id"
      tags:
        - "connection"
      operationId: "getConnection"
      parameters:
        - name: "id"
          description: "The id of a connection."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      responses:
        "200":
          description: "The connection object."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Connection"
    patch:
      description: "Update an existing connection."
      summary: "Update an existing connection"
      tags:
        - "connection"
      operationId: "updateConnection"
      parameters:
        - name: "id"
          description: "The id of a connection."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateConnectionRequest"
        required: true
      responses:
        "204":
          description: "Successful operation."
  /connections/{id}/tags:
    put:
      description: "Updates tags of an existing connection."
      summary: "Updates tags of an existing connection"
      tags:
        - "connection-tag"
      operationId: "updateConnectionTags"
      parameters:
        - name: "id"
          description: "The id of the connection."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTagsRequest"
        required: true
      responses:
        "204":
          description: "Successful operation."
  /project-deployments:
    post:
      description: "Create a new project deployment."
      summary: "Create a new project deployment"
      tags:
      - "project-deployment"
      operationId: "createProjectDeployment"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectDeployment"
        required: true
      responses:
        "200":
          description: "The project deployment id."
          content:
            application/json:
              schema:
                type: integer
                format: int64
  /project-deployments/tags:
    get:
      description: "Get project deployment tags."
      summary: "Get project deployment tags"
      tags:
      - "project-deployment-tag"
      operationId: "getProjectDeploymentTags"
      responses:
        "200":
          description: "The list of project deployment tags."
          content:
            application/json:
              schema:
                description: "The response object that contains the array of tags."
                type: "array"
                items:
                  $ref: "#/components/schemas/Tag"
  /project-deployments/{id}:
    delete:
      description: "Delete a project deployment."
      summary: "Delete a project deployment"
      tags:
      - "project-deployment"
      operationId: "deleteProjectDeployment"
      parameters:
      - name: "id"
        description: "The id of a project deployment."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      responses:
        "204":
          description: "Successful operation."
    get:
      description: "Get a project deployment by id."
      summary: "Get a project deployment by id"
      tags:
      - "project-deployment"
      operationId: "getProjectDeployment"
      parameters:
      - name: "id"
        description: "The id of a project deployment."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      responses:
        "200":
          description: "The project deployment object."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDeployment"
    put:
      description: "Update an existing project deployment."
      summary: "Update an existing project deployment"
      tags:
      - "project-deployment"
      operationId: "updateProjectDeployment"
      parameters:
      - name: "id"
        description: "The id of a project deployment."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectDeployment"
        required: true
      responses:
        "204":
          description: "Successful operation."
  /project-deployments/{id}/enable/{enable}:
    patch:
      description: "Enable/disable a project deployment."
      summary: "Enable/disable a project deployment"
      tags:
      - "project-deployment"
      operationId: "enableProjectDeployment"
      parameters:
      - name: "id"
        description: "The id of a project deployment."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      - name: "enable"
        description: "Enable/disable the project deployment."
        in: "path"
        required: true
        schema:
          type: "boolean"
      responses:
        "204":
          description: "Successful operation."
  /project-deployments/{id}/tags:
    put:
      description: "Updates tags of an existing project deployment."
      summary: "Updates tags of an existing project deployment"
      tags:
      - "project-deployment-tag"
      operationId: "updateProjectDeploymentTags"
      parameters:
      - name: "id"
        description: "The id of a project deployment."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTagsRequest"
        required: true
      responses:
        "204":
          description: "Successful operation."
  /project-deployments/{id}/workflows/{workflowId}/enable/{enable}:
    patch:
      description: "Enable/disable a workflow of a project deployment."
      summary: "Enable/disable a workflow of a project deployment"
      tags:
      - "project-deployment"
      operationId: "enableProjectDeploymentWorkflow"
      parameters:
      - name: "id"
        description: "The id of a project deployment."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      - name: "workflowId"
        description: "The id of a project workflow."
        in: "path"
        required: true
        schema:
          type: "string"
      - name: "enable"
        description: "Enable/disable the workflow of a project deployment."
        in: "path"
        required: true
        schema:
          type: "boolean"
      responses:
        "204":
          description: "Successful operation."
  /project-deployments/{id}/project-deployment-workflows/{projectDeploymentWorkflowId}:
    put:
      description: "Update an existing project deployment workflow."
      summary: "Update an existing project deployment workflow"
      tags:
        - "project-deployment"
      operationId: "updateProjectDeploymentWorkflow"
      parameters:
        - name: "id"
          description: "The id of a project deployment."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
        - name: "projectDeploymentWorkflowId"
          description: "The id of a project deployment workflow."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectDeploymentWorkflow"
        required: true
      responses:
        "204":
          description: "Successful operation."
  /project-deployments/{id}/workflows/{workflowId}/jobs:
    post:
      description: "Create a request for running a new job."
      summary: "Create a request for running a new job"
      tags:
      - "project-deployment"
      operationId: "createProjectDeploymentWorkflowJob"
      parameters:
      - name: "id"
        description: "The id of a project deployment."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      - name: "workflowId"
        description: "The id of the workflow to execute."
        in: "path"
        required: true
        schema:
          type: "string"
      responses:
        "200":
          description: "The id of a created job."
          content:
            application/json:
              schema:
                type: "object"
                properties:
                  jobId:
                    description: "The id of an executed job."
                    type: "integer"
                    format: "int64"
  /projects:
    post:
      description: "Create a new project."
      summary: "Create a new project."
      tags:
      - "project"
      operationId: "createProject"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Project"
        required: true
      responses:
        "200":
          description: "The project id."
          content:
            application/json:
              schema:
                type: integer
                format: int64
  /projects/categories:
    get:
      description: "Get categories."
      summary: "Get categories"
      tags:
      - "category"
      operationId: "getProjectCategories"
      responses:
        "200":
          description: "The list of categories."
          content:
            application/json:
              schema:
                description: "The response object that contains the array of categories."
                type: "array"
                items:
                  $ref: "#/components/schemas/Category"
  /projects/tags:
    get:
      description: "Get project tags."
      summary: "Get project tags."
      tags:
      - "project-tag"
      operationId: "getProjectTags"
      responses:
        "200":
          description: "The list of project tags."
          content:
            application/json:
              schema:
                description: "The response object that contains the array of tags."
                type: "array"
                items:
                  $ref: "#/components/schemas/Tag"
  /projects/{id}:
    delete:
      description: "Delete a project."
      summary: "Delete a project."
      tags:
      - "project"
      operationId: "deleteProject"
      parameters:
      - name: "id"
        description: "The id of a project."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      responses:
        "204":
          description: "Successful operation."
    get:
      description: "Get a project by id."
      summary: "Get a project by id."
      tags:
      - "project"
      operationId: "getProject"
      parameters:
      - name: "id"
        description: "The id of a project."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      responses:
        "200":
          description: "The project object."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
    put:
      description: "Update an existing project."
      summary: "Update an existing project."
      tags:
      - "project"
      operationId: "updateProject"
      parameters:
      - name: "id"
        description: "The id of a project."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Project"
        required: true
      responses:
        "204":
          description: "Successful operation."
  /projects/{id}/duplicate:
    post:
      description: "Duplicates existing project."
      summary: "Duplicates existing project."
      tags:
      - "project"
      operationId: "duplicateProject"
      parameters:
      - name: "id"
        description: "The id of a project."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      responses:
        "200":
          description: "The duplicated project object."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
  /projects/{id}/publish:
    post:
      description: "Publishes existing project."
      summary: "Publishes existing project."
      tags:
        - "project"
      operationId: "publishProject"
      parameters:
        - name: "id"
          description: "The id of a project."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                description:
                  description: The description of a project version.
                  type: "string"
      responses:
        "204":
          description: "Successful operation."
  /projects/{id}/export:
    get:
      description: "Export project as a zip file."
      summary: "Export project."
      tags:
        - "project"
      operationId: "exportProject"
      parameters:
        - name: "id"
          description: "The id of a project."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      responses:
        "200":
          description: "The project export file."
          content:
            application/octet-stream:
              schema:
                type: "string"
                format: "binary"
  /projects/{id}/versions:
    get:
      description: "Get a project versions."
      summary: "Get a project versions."
      tags:
        - "project"
      operationId: "getProjectVersions"
      parameters:
        - name: "id"
          description: "The id of a project."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      responses:
        "200":
          description: "The list of project version objects."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/ProjectVersion"
  /projects/{id}/versions/{projectVersion}/workflows:
    get:
      description: "Get workflows for particular project version."
      summary: "Get workflows for particular project version."
      tags:
        - "workflow"
      operationId: "getProjectVersionWorkflows"
      parameters:
        - name: "id"
          description: "The id of a project."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
        - name: "projectVersion"
          description: "The version of a project."
          in: "path"
          required: true
          schema:
            type: "integer"
        - name: "includeAllFields"
          description: "Use for including all fields or just basic ones."
          in: "query"
          required: false
          schema:
            type: "boolean"
            default: true
      responses:
        "200":
          description: "The array of project workflows."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Workflow"
  /projects/{id}/tags:
    put:
      description: "Updates tags of an existing project."
      summary: "Updates tags of an existing project."
      tags:
      - "project-tag"
      operationId: "updateProjectTags"
      parameters:
      - name: "id"
        description: "The id of a project."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTagsRequest"
        required: true
      responses:
        "204":
          description: "Successful operation."
  /projects/{id}/workflows:
    get:
      description: "Get workflows for particular project."
      summary: "Get workflows for particular project."
      tags:
      - "workflow"
      operationId: "getProjectWorkflows"
      parameters:
      - name: "id"
        description: "The id of a project."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      responses:
        "200":
          description: "The array of project workflows."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Workflow"
    post:
      description: "Create new workflow and adds it to an existing project."
      summary: "Create new workflow and adds it to an existing project."
      tags:
      - "workflow"
      operationId: "createProjectWorkflow"
      parameters:
      - name: "id"
        description: "The id of a project."
        in: "path"
        required: true
        schema:
          type: "integer"
          format: "int64"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Workflow"
        required: true
      responses:
        "200":
          description: "The project workflow id."
          content:
            application/json:
              schema:
                type: integer
                format: int64
  /projects/{id}/workflows/{workflowId}/duplicate:
    post:
      description: "Duplicates existing workflow."
      summary: "Duplicates existing workflow."
      tags:
        - "workflow"
      operationId: "duplicateWorkflow"
      parameters:
        - name: "id"
          description: "The id of a project."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
        - name: "workflowId"
          description: "The id of a workflow."
          in: "path"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          description: "The id of a new duplicated workflow object."
          content:
            application/json:
              schema:
                type: "object"
                properties:
                  id:
                    description: "The id of the duplicated workflow."
                    type: "string"
  /users/{id}/workspaces:
    get:
      description: "Get all user workspaces."
      summary: "Get all user workspaces"
      tags:
        - "workspace"
      operationId: "getUserWorkspaces"
      parameters:
        - name: "id"
          description: "The id of a user."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      responses:
        "200":
          description: "The list of user workspaces."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Workspace"
  /webhooks/{workflowId}/test/start:
    post:
      description: "Test a webhook trigger."
      summary: "Test a webhook trigger"
      tags:
        - "webhook-trigger-test"
      operationId: "startWebhookTriggerTest"
      parameters:
        - name: "workflowId"
          description: "The id of a workflow."
          in: "path"
          required: true
          schema:
            type: "string"
        - name: "environmentId"
          description: "The id of an environment."
          required: true
          in: "query"
          schema:
            type: "integer"
            format: "int64"
      responses:
        "200":
          description: "Returns the webhook URL."
          content:
            application/json:
              schema:
                type: "object"
                properties:
                  webhookUrl:
                    description: "The webhook URL."
                    type: "string"
  /webhooks/{workflowId}/test/stop:
    post:
      description: "Test a webhook trigger."
      summary: "Test a webhook trigger"
      tags:
        - "webhook-trigger-test"
      operationId: "stopWebhookTriggerTest"
      parameters:
        - name: "workflowId"
          description: "The id of a workflow."
          in: "path"
          required: true
          schema:
            type: "string"
        - name: "environmentId"
          description: "The id of an environment."
          required: true
          in: "query"
          schema:
            type: "integer"
            format: "int64"
      responses:
        "204":
          description: "Successful operation."
  /workflows:
    get:
      description: "Get all workflows."
      summary: "Get all workflows."
      tags:
        - "workflow"
      operationId: "getWorkflows"
      responses:
        "200":
          description: "The array of workflows."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Workflow"
  /workflows/{id}:
    delete:
      description: "Delete a workflow."
      summary: "Delete a workflow"
      tags:
        - "workflow"
      operationId: "deleteWorkflow"
      parameters:
        - name: "id"
          description: "The id of the workflow to delete."
          in: "path"
          required: true
          schema:
            type: "string"
      responses:
        "204":
          description: "Successful operation."
    get:
      description: "Get a workflow by id."
      summary: "Get a workflow by id"
      tags:
        - "workflow"
      operationId: "getWorkflow"
      parameters:
        - name: "id"
          description: "The id of the workflow to get."
          in: "path"
          required: true
          schema:
            type: "string"
      responses:
        "200":
          description: "The workflow object."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workflow"
    put:
      description: "Update an existing workflow."
      summary: "Update an existing workflow"
      tags:
        - "workflow"
      operationId: "updateWorkflow"
      parameters:
        - name: "id"
          description: "The id of the workflow to update."
          in: "path"
          required: true
          schema:
            type: "string"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Workflow"
        required: true
      responses:
        "204":
          description: "Successful operation."
  /workflows/by-project-workflow-id/{projectWorkflowId}:
    get:
      description: "Get workflow for particular project."
      summary: "Get workflow for particular project."
      tags:
        - "workflow"
      operationId: "getProjectWorkflow"
      parameters:
        - name: "projectWorkflowId"
          description: "The id of a project workflow."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      responses:
        "200":
          description: "The project workflow object."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workflow"
  /workspaces/{id}/connections:
    get:
      description: "Get all workspace connections."
      summary: "Get all workspace connections"
      tags:
        - "connection"
      operationId: "getWorkspaceConnections"
      parameters:
        - name: "id"
          description: "The id of a workspace."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
        - name: "componentName"
          description: "The component name used for filtering connections."
          in: "query"
          required: false
          schema:
            type: "string"
        - name: "connectionVersion"
          description: "The connection version."
          in: "query"
          required: false
          schema:
            type: "integer"
        - name: "environmentId"
          description: "The id of an environment."
          in: "query"
          required: false
          schema:
            type: "integer"
            format: "int64"
        - name: "tagId"
          description: "The tag id of used for filtering connections."
          in: "query"
          required: false
          schema:
            type: "integer"
            format: "int64"
      responses:
        "200":
          description: "The list of connections."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Connection"
  /workspaces/{id}/project-deployments:
    get:
      description: "Get project deployments."
      summary: "Get project deployments"
      tags:
        - "project-deployment"
      operationId: "getWorkspaceProjectDeployments"
      parameters:
        - name: "id"
          description: "The id of a workspace."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
        - name: "environmentId"
          description: "The id of an environment."
          in: "query"
          required: false
          schema:
            type: "integer"
            format: "int64"
        - name: "projectId"
          description: "The project ids used for filtering project deployments."
          in: "query"
          required: false
          schema:
            type: "integer"
            format: "int64"
        - name: "tagId"
          description: "The tag id of used for filtering project deployments."
          in: "query"
          required: false
          schema:
            type: "integer"
            format: "int64"
        - name: "includeAllFields"
          description: "Use for including all fields or just basic ones."
          in: "query"
          required: false
          schema:
            type: "boolean"
            default: true
      responses:
        "200":
          description: "The list of project deployments."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/ProjectDeployment"
  /workspaces/{id}/projects:
    get:
      description: "Get projects by workspace id."
      summary: "Get projects by workspace id"
      tags:
        - "project"
      operationId: "getWorkspaceProjects"
      parameters:
        - name: "id"
          description: "The id of a workspace."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
        - name: "apiCollections"
          description: "Use for filtering API Collection based projects."
          in: "query"
          required: false
          schema:
            type: "boolean"
        - name: "categoryId"
          description: "The category id used for filtering projects."
          in: "query"
          required: false
          schema:
            type: "integer"
            format: "int64"
        - name: "includeAllFields"
          description: "Use for including all fields or just basic ones."
          in: "query"
          required: false
          schema:
            type: "boolean"
            default: true
        - name: "projectDeployments"
          description: "Use for filtering projects for which project deployments exist."
          in: "query"
          required: false
          schema:
            type: "boolean"
        - name: "status"
          description: "Use for filtering projects per status."
          in: "query"
          required: false
          schema:
            $ref: '#/components/schemas/ProjectStatus'
        - name: "tagId"
          description: "The tag id of used for filtering projects."
          in: "query"
          required: false
          schema:
            type: "integer"
            format: "int64"
      responses:
        "200":
          description: "The list of projects."
          content:
            application/json:
              schema:
                type: "array"
                items:
                  $ref: "#/components/schemas/Project"
  /workspaces/{workspaceId}/projects/import:
    post:
      description: "Import project from a zip file."
      summary: "Import project."
      tags:
        - "project"
      operationId: "importProject"
      parameters:
        - name: "workspaceId"
          description: "The id of the workspace."
          in: "path"
          required: true
          schema:
            type: "integer"
            format: "int64"
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: "object"
              properties:
                file:
                  type: "string"
                  format: "binary"
              required:
                - "file"
      responses:
        "200":
          description: "The imported project id."
          content:
            application/json:
              schema:
                type: "integer"
                format: "int64"
components:
  schemas:
    Category:
      $ref: "../../../../platform/platform-category/platform-category-rest/openapi.yaml#/components/schemas/Category"
    Connection:
      description: "Contains all required information to open a connection to a service\
        \ defined by componentName parameter."
      allOf:
        - $ref: "../../../../platform/platform-connection/platform-connection-rest/openapi/components/schemas/objects/connection_base.yaml"
        - type: object
          properties:
            workspaceId:
              type: integer
              format: int64
    Project:
      description: "A group of workflows that make one logical project."
      allOf:
        - $ref: "#/components/schemas/ProjectBasic"
        - type: "object"
          required:
            - "name"
            - "workspaceId"
          properties:
            category:
              $ref: "#/components/schemas/Category"
            projectWorkflowIds:
              description: "The project workflow ids belonging to this project."
              type: "array"
              items:
                type: "integer"
                format: "int64"
            tags:
              type: "array"
              items:
                $ref: "#/components/schemas/Tag"
            workspaceId:
              description: "The workspace id."
              type: "integer"
              format: "int64"
            __version:
              type: "integer"
    ProjectBasic:
      description: "A group of workflows that make one logical project."
      type: "object"
      required:
      - "name"
      properties:
        createdBy:
          description: "The created by."
          type: "string"
          readOnly: true
        createdDate:
          description: "The created date."
          type: "string"
          format: "date-time"
          readOnly: true
        description:
          description: "The description of a project."
          type: "string"
        id:
          description: "The id of a project."
          type: "integer"
          format: "int64"
          readOnly: true
        lastModifiedBy:
          description: "The last modified by."
          type: "string"
          readOnly: true
        lastModifiedDate:
          description: "The last modified date."
          type: "string"
          format: "date-time"
          readOnly: true
        name:
          description: "The name of a project."
          type: "string"
        lastPublishedDate:
          description: "The last published date."
          type: "string"
          format: "date-time"
          readOnly: true
        lastStatus:
          $ref: '#/components/schemas/ProjectStatus'
        lastProjectVersion:
          description: "The last version of a project."
          type: "integer"
          readOnly: true
        uuid:
          description: "The uuid of a project."
          type: "string"
    ProjectDeployment:
      description: "Contains configurations and connections required for the execution\
        \ of project workflows."
      allOf:
        - $ref: "#/components/schemas/ProjectDeploymentBasic"
        - type: "object"
          required:
            - "name"
          properties:
            project:
              type: "object"
              allOf:
                - $ref: "#/components/schemas/ProjectBasic"
              readOnly: true
            projectDeploymentWorkflows:
              type: "array"
              items:
                $ref: "#/components/schemas/ProjectDeploymentWorkflow"
            tags:
              type: "array"
              items:
                $ref: "#/components/schemas/Tag"
            __version:
              type: "integer"
    ProjectDeploymentBasic:
      description: "Contains configurations and connections required for the execution\
        \ of project workflows."
      type: "object"
      required:
        - "name"
      properties:
        createdBy:
          description: "The created by."
          type: "string"
          readOnly: true
        createdDate:
          description: "The created date."
          type: "string"
          format: "date-time"
          readOnly: true
        description:
          description: "The description of a project deployment."
          type: "string"
        enabled:
          description: "If a project deployment is enabled or not."
          type: "boolean"
        environmentId:
          description: "The id of an environment."
          type: "integer"
          format: "int64"
        id:
          description: "The id of a project deployment."
          type: "integer"
          format: "int64"
          readOnly: true
        lastExecutionDate:
          description: "The last execution date."
          type: "string"
          format: "date-time"
          readOnly: true
        lastModifiedBy:
          description: "The last modified by."
          type: "string"
          readOnly: true
        lastModifiedDate:
          description: "The last modified date."
          type: "string"
          format: "date-time"
          readOnly: true
        name:
          description: "The name of a project deployment."
          type: "string"
        projectId:
          description: "The id of a project."
          type: "integer"
          format: "int64"
        projectVersion:
          description: "The version of a project."
          type: "integer"
    ProjectDeploymentWorkflow:
      description: "Contains configuration and connections required for the execution\
        \ of a particular project workflow."
      type: "object"
      required:
      - "name"
      properties:
        createdBy:
          description: "The created by."
          type: "string"
          readOnly: true
        createdDate:
          description: "The created date."
          type: "string"
          format: "date-time"
          readOnly: true
        inputs:
          description: "The input parameters of an project deployment used as workflow\
            \ input values."
          type: "object"
          additionalProperties: true
        connections:
          description: "The connections used by a project deployment."
          type: "array"
          items:
            $ref: "#/components/schemas/ProjectDeploymentWorkflowConnection"
        enabled:
          description: "If a workflow is enabled or not in the project deployment workflow."
          type: "boolean"
        id:
          description: "The id of a project deployment workflow."
          type: "integer"
          format: "int64"
          readOnly: true
        lastExecutionDate:
          description: "The last execution date of a project deployment."
          type: "string"
          format: "date-time"
        lastModifiedBy:
          description: "The last modified by."
          type: "string"
          readOnly: true
        lastModifiedDate:
          description: "The last modified date."
          type: "string"
          format: "date-time"
          readOnly: true
        projectDeploymentId:
          description: "The id of a project deployment."
          type: "integer"
          format: "int64"
        staticWebhookUrl:
          description: "The url of a static url used to trigger a workflow."
          type: "string"
        workflowId:
          description: "The id of a workflow."
          type: "string"
        workflowUuid:
          description: "The workflow uuid"
          type: "string"
          readOnly: true
        __version:
          type: "integer"
    ProjectDeploymentWorkflowConnection:
      description: "The connection used in a particular action task or trigger."
      type: "object"
      required:
        - "connectionId"
        - "workflowConnectionKey"
        - "workflowNodeName"
      properties:
        connectionId:
          description: "The connection id"
          type: "integer"
          format: "int64"
        workflowConnectionKey:
          description: "The connection key under which a connection is defined in\
            \ a workflow definition."
          type: "string"
        workflowNodeName:
          description: "The action/trigger name to which a connection belongs."
          type: "string"
    ProjectStatus:
      description: "The status of a project."
      type: "string"
      enum:
        - "DRAFT"
        - "PUBLISHED"
    ProjectVersion:
      description: "The project version."
      type: "object"
      properties:
        description:
          description: "The description of a project version"
          type: "string"
        publishedDate:
          description: "The published date."
          type: "string"
          format: "date-time"
        version:
          description: "The version of a project."
          type: "integer"
          readOnly: true
        status:
          $ref: '#/components/schemas/ProjectStatus'
    Tag:
      $ref: "../../../../platform/platform-tag/platform-tag-rest/openapi/components/schemas/objects/tag.yaml"
    UpdateConnectionRequest:
      description: "Contains all connection parameters that can be updated."
      type: "object"
      required:
        - "name"
        - "tags"
      properties:
        name:
          description: "The name of a connection."
          type: "string"
        tags:
          type: "array"
          items:
            $ref: "#/components/schemas/Tag"
        __version:
          type: "integer"
          format: "int32"
    UpdateTagsRequest:
      description: "The request object that contains the array of tags."
      type: "object"
      properties:
        tags:
          type: "array"
          items:
            $ref: "#/components/schemas/Tag"
    Workflow:
      description: "The blueprint that describe the execution of a job."
      x-implements: ['com.bytechef.platform.configuration.web.rest.model.WorkflowModelAware']
      allOf:
        - $ref: "../../../../platform/platform-configuration/platform-configuration-rest/platform-configuration-rest-impl/openapi.yaml#/components/schemas/Workflow"
        - type: "object"
          properties:
            projectWorkflowId:
              description: "The project workflow id"
              type: "integer"
              format: "int64"
              readOnly: true
            sseStreamResponse:
              description: "Indicates if the workflow has SSE stream response"
              type: "boolean"
              readOnly: true
            workflowUuid:
              description: "The workflow uuid"
              type: "string"
              readOnly: true
    WorkflowBasic:
      description: "The blueprint that describe the execution of a job."
      allOf:
        - $ref: "../../../../platform/platform-configuration/platform-configuration-rest/platform-configuration-rest-impl/openapi.yaml#/components/schemas/WorkflowBasic"
        - type: "object"
          properties:
            projectWorkflowId:
              description: "The project workflow id"
              type: "integer"
              format: "int64"
              readOnly: true
            workflowUuid:
              description: "The workflow uuid"
              type: "string"
              readOnly: true
    Workspace:
      description: "A workspace."
      required:
        - "name"
      properties:
        createdBy:
          description: "The created by."
          type: "string"
          readOnly: true
        createdDate:
          description: "The created date."
          type: "string"
          format: "date-time"
          readOnly: true
        description:
          description: "The description of a workspace."
          type: "string"
        id:
          description: "The id of the category."
          type: "integer"
          format: "int64"
        name:
          description: "The name of the category."
          type: "string"
        lastModifiedBy:
          description: "The last modified by."
          type: "string"
          readOnly: true
        lastModifiedDate:
          description: "The last modified date."
          type: "string"
          format: "date-time"
          readOnly: true
        __version:
          type: "integer"
          format: "int32"
